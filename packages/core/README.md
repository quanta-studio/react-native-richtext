# @yk-yong/rn-rich-text-core

Render-model builder for rn-rich-text: turns a parsed DOM + per-element computed
styles into a renderer-agnostic styled tree (block/inline split, whitespace
collapse, entity decode, list markers). React-free pure logic.

Not yet published — internal to the rn-rich-text monorepo.
