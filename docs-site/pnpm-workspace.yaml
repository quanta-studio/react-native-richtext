# Standalone workspace root so `pnpm install` here does not climb to the library's
# root workspace. docs-site is intentionally NOT a member of the library workspace.
packages: []
allowBuilds:
  esbuild: true
  sharp: true
